import { Center } from '../Center';
import { Section } from '../Section';

export const Title = () => {
  return (
    <Center>
      <Section>
        <h1>Title</h1>
      </Section>
    </Center>
  );
};
