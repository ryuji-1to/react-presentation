import { Center } from '../template/Center';
import { Section } from '../template/Section';

export const Title = () => {
  return (
    <Center>
      <Section>
        <h1>Title</h1>
      </Section>
    </Center>
  );
};
